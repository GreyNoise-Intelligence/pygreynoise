repos:
  - repo: local
    hooks:
      - id: black
        name: Style check (black)
        entry: black
        language: system
        types:
          - python
      - id: flake8
        name: Static analysis (flake8)
        entry: flake8
        language: system
        types:
          - python
